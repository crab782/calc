这是什么项目


项目最终假如要托管到github page的化，是最后打包出的dist文件夹，本地运行怎样，托管就怎样的吗？


`spec` 需要做一个记账工具，由于只托管静态文件，这工具用localstorage作为数据库
暂时先做一个总览页，网址是“localhost:5173/”这样，默认进入总览页
总体网页类似后台管理项目一样，左侧栏能点击其他页面模块等等，暂时只做一个记账页


在localstorage里存数据，是json格式还是怎样的

是否有数据库这个概念，比如表 字段等等


`plan` 怎么做导入 导出功能好，这是在浏览器的，需要导入和导出功能

希望添加一个“设置”页面，添加一个能够导入 导出为json的功能


需要总览添加一个图表，显示为折线图，按月显示情况


是否有必要使用普通的echart，听说这个性能比较好，假如是就换为这个


`spec` 希望效仿java项目的经典mvc架构，方便跟踪究竟存了什么表 字段在localstorage，并且希望能够保证json导入 导出是完整的

什么方案比较好，希望用行业主流的规范方案，合理架构分层，弄个”数据操作层“？这种东西


怎么确保localstorage只会存一个json，和使用一个json，避免项目用到很多个这样
目前的设计是只用一个的吗

怎么开发阶段记录需要的“表” “字段”，是否需要完整效仿java项目那种架构分层，把实体类都弄一个？

暂时不要修改代码，行业主流是怎么做比较合适


`spec` I need to revise the instruction for the  operation. After reconsideration, I've decided to abandon the previously proposed MVC-inspired layered architecture. While localStorage operations should still be encapsulated in a dedicated file, I want the overall directory structure to strictly follow established conventions in the mainstream TypeScript frontend ecosystem.

Please implement a directory structure that aligns with current industry best practices for TypeScript-based frontend projects, avoiding non-standard patterns like separate "dao" and "model" directories that might cause confusion in collaborative development environments. The architecture should follow conventional organizational patterns recognized by most frontend developers familiar with modern TypeScript workflows.


有必要弄个hook目录吗，不过这是指望最后打包静态页面然后托管到github page的，数据放localstorage

希望主流和规范的设计，方便多人开发快速看懂项目结构和目录


`plan` 还是希望添加hooks目录，需要这个
并且生成AGENTS.md，记录项目的框架和目录结构等等


修改导出json的文件名称，希望简单为account-book.json这样就好了，不带时间


`spec` 项目需要弄一个英文支持，总览页需要弄一个按钮，可以切换中文(默认)和english这样


设置是放在localstorage好还是放在cookie好，希望可以放在cookie？主流的做法是怎样的


首页总览页的图表，希望业务逻辑如下
1 即使用户没输入过去的财务情况（比如输入了5 6月，没有输入过4月往前），还是能够断言一个4月往前的财务情况，就假设为4月既没有发生支出，也没有发生收入就可以了，这样图表能继续显示4月和3月的数据
2 同理，预测的未来还没发生的，也可以断言预算，假设为7月（未来）还没发生支出和收入，就7月保持6月的财务情况

3 首页的总览图需要拆分为3个图，一个是总财务情况(结余)，下面是支出，收入这样
4 总览图默认为显示过去半年和未来半年

这样的目的是后续方便添加预测收入 预测支出等等功能

使用子代理执行，确保任务完整进行


使用子代理执行，确保任务完整进行
1 总览页面这三张表，收入和支出图表需要保持和总资产图表一样的宽度，如图一所示，就把收入和支出表依次放在下面
2 预测的未来月份用虚线，但是过去月份还是按用户输入为准(因为这是记账部分了，属于过去，没有必要假设)，假如用户没有输入过，就保持留空为0
3 并不要现在假如预测现金流，而是这样
a 未来的月份：除非是有自动规则(比如工资等稳定现金流收入，用户在规则页(目前还没有，后续添加)手动设置的规则等等)，算进的收入，才加进来(相当于是预期收入)，否则都直接认为是无事发生：即在未来的7 8月份既没有收入，也没有支出，这个图表应该是直线的
b 预算(预测)仅限后面添加的规则页，里面详细设置各种参数配置等等，而目前还不需要画蛇添足进行预算


如图一所示
1 希望取消掉折线图这个圆滑效果，就折线图这样显示折线可以了
2 实线和虚线这个月中间断开了，希望能连续回来






























